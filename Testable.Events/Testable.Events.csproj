<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="12.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import Project="$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props" Condition="Exists('$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props')" />
  <PropertyGroup>
    <AppDesignerFolder>Properties</AppDesignerFolder>
    <RootNamespace>Testable.Events</RootNamespace>
    <AssemblyName>Testable.Events</AssemblyName>
    <SolutionDir Condition="$(SolutionDir) == '' Or $(SolutionDir) == '*Undefined*'">..\</SolutionDir>
    <RestorePackages>true</RestorePackages>
    <NugetVersionName Condition=" '$(NugetVersionName)' == '' ">net20\</NugetVersionName>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(FrameworkSpecific)' == '' ">
    <ProjectGuid>{9BED67F6-5BF9-4838-9574-AFE226B48FEA}</ProjectGuid>
    <OutputType>Library</OutputType>
    <TargetFrameworkVersion>v4.5</TargetFrameworkVersion>
    <FileAlignment>512</FileAlignment>
    <RestorePackages>false</RestorePackages>
  </PropertyGroup>
  <ItemGroup Condition=" '$(FrameworkSpecific)' == '' ">
    <Reference Include="System" />
    <Reference Include="System.Core" />
    <Reference Include="System.Xml.Linq" />
    <Reference Include="System.Data.DataSetExtensions" />
    <Reference Include="Microsoft.CSharp" />
    <Reference Include="System.Data" />
    <Reference Include="System.Xml" />
  </ItemGroup>
  <ItemGroup>
    <Reference Include="Contract.Annotations">
      <HintPath>..\packages\Contract.Annotations.8.2.1.0\lib\$(NugetVersionName)Contract.Annotations.dll</HintPath>
    </Reference>
  </ItemGroup>
  <ItemGroup>
    <Compile Include="Class1.cs" />
  </ItemGroup>
  <ItemGroup Condition="'$(FrameworkSpecific)' == ''">
    <Compile Include="Properties\AssemblyInfo.cs" />
  </ItemGroup>
  <ItemGroup>
    <None Include="Package.bat" />
    <None Include="Testable.Events.Net45.nuspec" />
    <None Include="packages.config">
      <SubType>Designer</SubType>
    </None>
  </ItemGroup>
  <Target Name="ShowStuff">
    <!--		<Message Text="specific: $(FrameworkSpecific)" importance="high" />
		<Message Text="references: @(Reference)" importance="high" />
-->
  </Target>
  <Target Name="Build">
    <CallTarget Targets="ShowStuff" />
  </Target>
  <Target Name="Clean" />
  <Target Name="Rebuild" />
  <Target Name="EnsureNuGetPackageBuildImports" BeforeTargets="PrepareForBuild">
    <PropertyGroup>
      <ErrorText>This project references NuGet package(s) that are missing on this computer. Enable NuGet Package Restore to download them.  For more information, see http://go.microsoft.com/fwlink/?LinkID=322105. The missing file is {0}.</ErrorText>
    </PropertyGroup>
    <Error Condition="!Exists('$(SolutionDir)\.nuget\NuGet.targets')" Text="$([System.String]::Format('$(ErrorText)', '$(SolutionDir)\.nuget\NuGet.targets'))" />
  </Target>
</Project>